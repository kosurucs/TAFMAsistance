# ── LLM Fine-tuning dependencies ─────────────────────────────────────────────
# Install: pip install -r llm_training/requirements.txt
#
# Requires: Python 3.10+, NVIDIA GPU with 8 GB+ VRAM (CUDA 11.8 or 12.x)

# Core training stack (Unsloth = fastest QLoRA on consumer GPUs)
unsloth[colab-new]>=2024.11  # auto-selects CUDA version
transformers>=4.45.0
trl>=0.12.0
peft>=0.13.0
bitsandbytes>=0.44.0
accelerate>=0.34.0
datasets>=3.0.0
tokenizers>=0.20.0

# PDF extraction
pymupdf>=1.24.0          # fitz – fast, accurate PDF text extraction

# Web scraping
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
tqdm>=4.66.0

# Data utilities
pandas>=2.2.0
numpy>=1.26.0

# Optional: GPU memory profiling
pynvml>=11.5.0
